# 更新记录

> 本文档记录 2026-09-13 ~ 09-14 四次迭代的全部更改，对应提交 `26dfe0c`、`b91a9c0` 与后续提交。

## 迭代四：侧栏改版 + 交互增强（本次）

- **天气注明城市**：中央天气显示为「20°C 西安 · 晴」格式；点击天气区域可手动刷新（图标旋转提示）。
- **首页入口图标化**：侧栏去掉「首页」文字项，改为小屋图标按钮，放在顶部折叠按键（‹）左侧；当前在首页时图标高亮；侧栏收起时头部自动变为纵向双图标排列。
- **一级菜单可折叠**：「收藏」「工具」标题变为可点击的折叠开关，带箭头指示与 0.3s 高度过渡动画，折叠状态存 localStorage 记忆。
- **新增交互效果**（悬停 + 点击全覆盖）：
  - 点击涟漪：侧栏所有可点元素（分类项、折叠标题、小屋、折叠键、主题按钮）点击时从按点扩散涟漪
  - 按压反馈：链接卡片按下缩放、各图标按钮按下缩小、GitHub 传送门按下微缩
  - 悬停动画：链接卡片图标放大旋转、标题变主题色；搜索框聚焦出现光晕
  - 主题切换时图标旋转一圈；移动端菜单按钮增加悬停/按压反馈
- 修改文件：`index.html`、`script.js`、`styles.css`

## 迭代三：首页去卡片化

- **取消卡片式**：仪表盘不再使用六宫格卡片，改为全屏沉浸式布局。
- **时钟 + 天气居中**：巨型时钟（随视口自适应）与日期、天气作为主视觉居中显示。
- **其余组件四角融入**（HUD 风格，无边框无底色）：右上 GitHub 传送门（含实时仓库/粉丝数）、左下时光进度、底部中央今日一言、右下待办清单。
- **响应式**：窄屏（≤1150px）四角组件自动改为纵向流式排布。

## 迭代二：全屏首页仪表盘 + 交互动画

- **仪表盘完整占据首页**：进入网站默认只显示仪表盘，下方不再出现任何收藏卡片。
- **搜索逻辑适配**：首页直接输入关键词自动切换到收藏搜索，清空后回到首页。
- **新增组件**：今日一言（Hitokoto）、时光进度（年/月/周）、待办清单（localStorage 持久化）、GitHub 实时数据（数字滚动）。
- **入场/悬停/微交互动画**：fadeUp/fadeDown 错落入场、卡片图标悬停、待办增删勾选动画、进度条生长等。
- 修改文件：`index.html`、`script.js`、`styles.css`；新增 `GitHub资源整理.md`、`更新记录.md`。

## 迭代一：基础功能（提交 26dfe0c）

- **一方工具箱**（<https://tool.fologde.com/>）加入「工具合集」分类。
- **侧栏调序**：「收藏」移至侧栏第一位，「工具」第二位。
- **首页仪表盘（初版）**：时钟、西安实时天气（Open-Meteo）、GitHub 传送门。
- **GitHub 搜索整理**（每类 ≥5 条，全部验证可访问）：
  - 「工具合集」+8：IT Tools、CyberChef、Omni Tools、IT Toolbox、傲星工具箱、彩虹工具箱、FreeTool、一方工具箱
  - 新增「开源工具」分类 +6：Excalidraw、tldraw、drawDB、Squoosh、LibreSpeed、WebStack 导航

## 技术说明

- 所有接口均免费无需密钥：天气 [Open-Meteo](https://open-meteo.com/)、一言 [Hitokoto](https://hitokoto.cn/)、GitHub REST API（未认证限额 60 次/小时/IP，个人使用足够）。
- 待办清单、主题偏好、侧栏状态、菜单折叠状态均存于浏览器 localStorage。
