# 更新记录

> 本文档记录 2026-09-13 三次迭代的全部更改，对应提交 `26dfe0c` 与后续提交。

## 迭代三：首页去卡片化（本次）

- **取消卡片式**：仪表盘不再使用六宫格卡片，改为全屏沉浸式布局。
- **时钟 + 天气居中**：巨型时钟（随视口自适应）与日期、天气（图标 + 温度 + 天气现象 + 今日温度/湿度/风速）作为主视觉居中显示。
- **其余组件四角融入**（HUD 风格，无边框无底色）：
  - 右上：GitHub 传送门（图标 + 用户名 + 实时仓库/粉丝数）
  - 左下：时光进度（今年/本月/周三条渐变进度条）
  - 底部中央：今日一言（淡入淡出换句）
  - 右下：待办清单（下划线输入框、悬停高亮的行式列表）
- **动画适配**：主视觉上浮入场，四角组件分别从各自方向淡入；其余交互动画保留。
- **响应式**：窄屏（≤1150px）四角组件自动改为纵向流式排布，时钟居中保持。

## 迭代二：全屏首页仪表盘 + 交互动画

### 首页改版

- **仪表盘完整占据首页**：进入网站默认只显示仪表盘，下方不再出现任何收藏卡片。
- **新增「首页」导航项**：侧栏最顶部新增 🏠 首页入口；点击分类/工具后可随时回到首页；按 `ESC` 也能一键返回。
- **搜索逻辑适配**：在首页直接输入关键词会自动切换到收藏搜索并隐藏仪表盘，清空搜索框后自动回到首页。

### 新增组件

| 组件 | 说明 |
|---|---|
| 今日一言 | Hitokoto 免费接口随机句子（诗词/哲学/文学），可一键换一句 |
| 时光进度 | 今年 / 本月 / 本周的时间进度条，百分比数字滚动动画 |
| 待办清单 | 支持添加、勾选完成、删除，数据存 localStorage 刷新不丢失 |
| GitHub 数据 | 传送门升级：实时显示公开仓库数、关注者数（GitHub API），数字滚动动画 |

### 交互动画

- **入场动画**：仪表盘组件错落入场（fadeUp / fadeDown + 逐个延迟）；切换分类时链接卡片同样错落入场。
- **悬停效果**：侧栏导航项悬停右滑；天气图标悬停放大旋转；GitHub 图标变色放大。
- **微交互**：一言刷新按钮点击旋转一圈、文字淡出淡入；待办项新增下滑入场、删除右滑淡出、勾选弹跳对勾；进度条加载时从 0 平滑生长；主题图标悬停摇摆。

### 修改文件

- `index.html`：侧栏加首页项；仪表盘全屏结构
- `script.js`：视图管理重构（home/links/tools 三态）；新增一言、时光进度、GitHub 数据、待办清单四个模块；搜索与 ESC 行为调整
- `styles.css`：仪表盘全屏布局；全部组件样式与动画关键帧；侧栏/卡片微交互

---

## 迭代一：基础功能（提交 26dfe0c）

- **一方工具箱**（<https://tool.fologde.com/>）加入「工具合集」分类。
- **侧栏调序**：「收藏」移至侧栏第一位，「工具」第二位。
- **首页仪表盘（初版）**：时钟（每秒刷新）、西安实时天气（Open-Meteo 免费接口，含今日温度/湿度/风速）、GitHub 传送门。
- **GitHub 搜索整理**（每类 ≥5 条，全部验证可访问）：
  - 「工具合集」+8：IT Tools、CyberChef、Omni Tools、IT Toolbox、傲星工具箱、彩虹工具箱、FreeTool、一方工具箱
  - 新增「开源工具」分类 +6：Excalidraw、tldraw、drawDB、Squoosh、LibreSpeed、WebStack 导航
- 修改文件：`index.html`、`script.js`、`styles.css`、`data/links.json`、`README.md`

## 技术说明

- 所有接口均免费无需密钥：天气 [Open-Meteo](https://open-meteo.com/)、一言 [Hitokoto](https://hitokoto.cn/)、GitHub REST API（未认证限额 60 次/小时/IP，个人使用足够）。
- 待办清单、主题偏好、侧栏状态均存于浏览器 localStorage。
